<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li> id <%= link_to task.id, task %> ステータス : <%= task.status %> タスク : <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>

<p>ログイン完了ユーザ: <%= current_user.name %></p>